#include <string>
#include <ctime>

namespace YandexSchedule {
    namespace Utils {
        std::string today();

        std::string toString(bool value);
        std::string toString(uint16_t number);
    };
};